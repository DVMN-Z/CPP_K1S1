#include <iostream>
#include <math.h>

using namespace std;

void Lab2(int variant)
{
	float a, b, t, y, x, c, r, l, s, d, f, z, h, w, m, k, n, g;

	if (variant == 1) {
		cout << "======================== Вариант #1 ========================\n" << endl;
		a = -0.5; b = 2;
		for (t = 2; t <= 5; t = t + 0.5)
		{
			y = exp(fabs(a + cos(b * t)));
			cout << "Y = " << y << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 2) {
		cout << "======================== Вариант #2 ========================\n" << endl;
		a = 3.5; y = 0;
		for (x = 1; x <= 10; x++)
		{
			y = 0.5 * x * pow((1 + a * x), 0.2);
			cout << "Y = " << y << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 3) {
		cout << "======================== Вариант #3 ========================\n" << endl;
		a = 2.5; y = 0;
		for (m = 1; m <= 10; m++)
		{
			y = exp(fabs(a + m));
			cout << "Y = " << y << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 4) {
		cout << "======================== Вариант #4 ========================\n" << endl;
		x = 0.5; c = 0;
		for (y = 1; y <= 8; y = y + 0.5)
		{
			c = pow(2, x + y);
			cout << "C = " << c << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 5) {
		cout << "======================== Вариант #5 ========================\n" << endl;
		t = 0; x = 2;
		for (a = 10; a <= 20; a = a + 2)
		{
			t = pow(sin(x), 3) + a * x;
			cout << "T = " << t << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 6) {
		cout << "======================== Вариант #6 ========================\n" << endl;
		t = 0; b = 2.5;
		for (a = 2; a <= 5; a = a + 0.5)
		{
			t = exp(fabs(a + b)) - pow(3, a);
			cout << "T = " << t << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 7) {
		cout << "======================== Вариант #7 ========================\n" << endl;
		r = 0; a = 0.2;
		for (c = 0.4; c <= 2.4; c = c + 0.4)
		{
			r = tan(pow(a, 3) + c);
			cout << "R = " << r << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 8) {
		cout << "======================== Вариант #8 ========================\n" << endl;
		x = 0; a = 0.2;
		for (c = 2; c <= 5; c = c + 0.5)
		{
			x = log(pow(a, 2) + pow(c, 3));
			cout << "X = " << x << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 9) {
		cout << "======================== Вариант #9 ========================\n" << endl;
		l = 0; a = 0.4; b = 1.3;
		for (x = 0.1; x <= 1.0; x = x + 0.1)
		{
			l = pow((b / a), x) + pow(cos(x / a), 2);
			cout << "L = " << l << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 10) {
		cout << "======================== Вариант #10 ========================\n" << endl;
		x = 5; t = 0;
		for (a = 10; a <= 20; a = a + 5)
		{
			t = pow(sin(a * x), 4);
			cout << "T = " << t << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 11) {
		cout << "======================== Вариант #11 ========================\n" << endl;
		x = 2; s = 0;
		for (a = 10; a <= 20; a = a + 2)
		{
			s = pow(x, a) + tan(a * x);
			cout << "S = " << s << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 12) {
		cout << "======================== Вариант #12 ========================\n" << endl;
		k = 0; b = 2;
		for (a = 0.2; a <= 1.6; a = a + 0.2)
		{
			k = sin(pow(b, 3) + a);
			cout << "K = " << k << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 13) {
		cout << "======================== Вариант #13 ========================\n" << endl;
		a = 0.2; d = 0;
		for (c = 0.7; c <= 2.1; c = c + 0.3)
		{
			d = pow(4, a * c) - pow(sin(a + c), 2);
			cout << "D = " << d << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 14) {
		cout << "======================== Вариант #14 ========================\n" << endl;
		f = 0; a = -2; b = 5;
		for (x = 2; x <= 5; x = x + 0.5)
		{
			f = fabs(pow(cos(a + pow(b, 2)), b)) + pow(x, 3);
			cout << "F = " << f << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 15) {
		cout << "======================== Вариант #15 ========================\n" << endl;
		z = 0; x = 1.5; a = 0.2;
		for (y = 1; y <= 3; y = y + 0.5)
		{
			z = fabs(exp(a * x) - y);
			cout << "Z = " << z << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 16) {
		cout << "======================== Вариант #16 ========================\n" << endl;
		l = 0; a = 0.5;
		for (y = 1; y <= 10; y++)
		{
			l = pow(sin(y), 3) + pow(a, y);
			cout << "L = " << l << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 17) {
		cout << "======================== Вариант #17 ========================\n" << endl;
		a = 2; z = 0;
		for (b = 5; b <= 8; b = b + 0.5)
		{
			z = exp(fabs(a + b)) - sin(a * b);
			cout << "Z = " << z << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 18) {
		cout << "======================== Вариант #18 ========================\n" << endl;
		k = 0; n = 2.5;
		for (m = 0.2; m <= 1.2; m = m + 0.2)
		{
			k = log(pow(n, 3) + pow(m, 2)) - tan(n * m);
			cout << "K = " << k << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 19) {
		cout << "======================== Вариант #19 ========================\n" << endl;
		h = 0; a = 1.1;
		for (x = 0.1; x <= 1.0; x = x + 0.1)
		{
			h = pow(2, a * x) - pow(cos(a + x), 3);
			cout << "H = " << h << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 20) {
		cout << "======================== Вариант #20 ========================\n" << endl;
		s = 0; a = 3;
		for (x = 0.5; x <= 2.5; x = x + 0.5)
		{
			s = fabs(pow(sin(pow(x, 5)), a)) - pow(x, 3);
			cout << "S = " << s << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 21) {
		cout << "======================== Вариант #21 ========================\n" << endl;
		t = 0; x = 1.5;
		for (a = 0.1; a <= 2.0; a = a + 0.5)
		{
			t = sin(a * x) + pow(2, a * x);
			cout << "T = " << t << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 22) {
		cout << "======================== Вариант #22 ========================\n" << endl;
		r = 0; x = 2;
		for (a = 10; a <= 20; a = a + 2)
		{
			r = sqrt(x * a) + tan(x * a);
			cout << "R = " << r << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 23) {
		cout << "======================== Вариант #23 ========================\n" << endl;
		g = 0; b = 2;
		for (a = 0.2; a <= 1.6; a = a + 0.2)
		{
			g = sin(b + a) + pow(a, 3);
			cout << "G = " << g << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 24) {
		cout << "======================== Вариант #24 ========================\n" << endl;
		a = 0.2; w = 0;
		for (c = 0.7; c <= 2.1; c = c + 0.3)
		{
			w = pow(4, a * c) - sin(a + c) + exp(a * c);
			cout << "W = " << w << endl;
		}
		cout << "\n============================================================" << endl;
	}
	else if (variant == 25) {
		cout << "======================== Вариант #25 ========================\n" << endl;
		f = 0; a = -2; b = 5; c = 0.7;
		for (x = 2; x <= 5; x = x + 0.5)
		{
			f = cos(a + b * x) - pow(x, 2 * c);
			cout << "F = " << f << endl;
		}
		cout << "\n============================================================" << endl;
	}
	
}